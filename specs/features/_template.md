# LFPT-XXX: <краткое название фичи>

## Статус
draft | approved | in-progress | testing | done

## Источник
Ссылка на исходный клиентский запрос (`specs/requests/LFPT-XXX-*.md`) или issue. Если спека написана напрямую без клиентского запроса — указать это явно.

## Контекст / зачем
Почему это нужно, какая проблема решается или какая ценность добавляется. 2-4 предложения.

## Связь с MASTER.md
Какие разделы [specs/MASTER.md](../MASTER.md) затрагивает эта фича — какие инварианты использует, какие (если есть) собирается изменить. Если фича меняет инвариант мастер-спеки — явно об этом сказать и обновить MASTER.md в том же PR.

## Требования

### Функциональные
- ...

### Нефункциональные
- Производительность / нагрузка (если применимо)
- i18n (какие новые строки, для es/ru/en)
- Безопасность (если есть новая точка входа данных от пользователя)

## Вне скоупа
Явно перечислить, что НЕ делаем в рамках этой фичи, даже если оно напрашивается.

## Изменения в системе

### API
Новые/изменённые endpoints (метод, URL, что делает).

### БД
Новая Liquibase-миграция `v1.XX-<description>.yaml`? Какие таблицы/колонки.

### UI
Какие шаблоны/страницы затронуты (Thymeleaf-файл, URL).

## Критерии приёмки
- [ ] Testable-пункт 1 (конкретный сценарий → ожидаемый результат)
- [ ] Testable-пункт 2
- [ ] ...

## Edge cases
- Что происходит при пустом/некорректном вводе
- Конкурентный доступ (если релевантно — 2 реплики в Docker Swarm)
- Что происходит для уже существующих данных (миграция/бэкфилл)

## Открытые вопросы
Вопросы, требующие ответа заказчика или архитектора до начала реализации. Пока список не пуст — статус не может быть `approved`.
