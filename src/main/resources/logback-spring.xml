<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Подключаем стандартные настройки Spring Boot -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- ========== МАСКИРОВЩИК ЧУВСТВИТЕЛЬНЫХ ДАННЫХ ========== -->
    <conversionRule conversionWord="mask"
                    converterClass="com.padle.core.padelcoreservice.logging.MaskingConverter"/>

    <!-- Свойства из application.yml/properties -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="padel-core"/>
    <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="logs"/>
    <springProperty scope="context" name="LOG_MAX_SIZE" source="logging.file.max-size" defaultValue="10MB"/>
    <springProperty scope="context" name="LOG_MAX_HISTORY" source="logging.file.max-history" defaultValue="30"/>
    <springProperty scope="context" name="LOG_TOTAL_SIZE_CAP" source="logging.file.total-size-cap" defaultValue="1GB"/>

    <!-- Переменные окружения (с приоритетом над свойствами) -->
    <property name="LOG_DIR" value="${LOG_PATH:-${java.io.tmpdir}/logs}"/>
    <property name="MAX_FILE_SIZE" value="${LOG_MAX_SIZE:-10MB}"/>
    <property name="MAX_HISTORY" value="${LOG_MAX_HISTORY:-30}"/>
    <property name="TOTAL_SIZE_CAP" value="${LOG_TOTAL_SIZE_CAP:-1GB}"/>

    <!-- ========== АППЕНДЕРЫ ========== -->

    <!-- Консольный аппендер (всегда активен) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Файловый аппендер для всех логов -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Аппендер для ошибок (отдельный файл) -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP_NAME}-error.log</file>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Аппендер для JSON логов (для аналитики, опционально) -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP_NAME}-json.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}-json.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>

        <encoder class="ch.qos.logback.classic.encoder.JsonEncoder"/>
    </appender>

    <!-- ========== ЛОГГЕРЫ ========== -->

    <!-- Логгер для самого приложения -->
    <logger name="com.padle.core" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
        <appender-ref ref="JSON_FILE"/>
        <appender-ref ref="LOKI"/>
    </logger>

    <!-- Логгер для ошибок (дублирует ERROR_FILE) -->
    <logger name="ERROR_LOGGER" level="ERROR" additivity="false">
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- SQL логи (отключаем по умолчанию) -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>

    <!-- Spring Security (только предупреждения) -->
    <logger name="org.springframework.security" level="WARN"/>

    <!-- Spring Framework (только предупреждения) -->
    <logger name="org.springframework" level="WARN"/>

    <!-- Hibernate (только предупреждения) -->
    <logger name="org.hibernate" level="WARN"/>

    <!-- ========== ПРОФАЙЛЫ ========== -->

    <springProfile name="dev,development,default">
        <logger name="com.padle.core" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql" level="TRACE"/>
    </springProfile>

    <springProfile name="prod,production">
        <logger name="com.padle.core" level="INFO"/>
        <logger name="org.springframework.security" level="WARN"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>

        <logger name="com.padle.core.service.PaymentService" level="INFO" additivity="false">
            <appender-ref ref="JSON_FILE"/>
        </logger>
    </springProfile>

    <springProfile name="staging">
        <logger name="com.padle.core" level="DEBUG"/>
        <logger name="org.springframework.security" level="INFO"/>
    </springProfile>

    <!-- ========== LOKI АППЕНДЕР ========== -->
    <springProperty scope="context" name="LOKI_URL" source="logging.loki.url" defaultValue="http://localhost:3100/loki/api/v1/push"/>
    <springProperty scope="context" name="LOKI_ENABLED" source="logging.loki.enabled" defaultValue="false"/>
    <springProperty scope="context" name="LOKI_BATCH_SIZE" source="logging.loki.batchMaxItems" defaultValue="1000"/>
    <springProperty scope="context" name="LOKI_BATCH_TIMEOUT" source="logging.loki.batchTimeoutMs" defaultValue="5000"/>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${LOKI_URL}</url>
            <auth>
                <username>${LOKI_USERNAME:-}</username>
                <password>${LOKI_PASSWORD:-}</password>
            </auth>
        </http>
        <format>
            <label>
                <pattern>app=${APP_NAME},host=${HOSTNAME},level=%level,environment=${ENVIRONMENT:-local}</pattern>
                <readMarkers>true</readMarkers>
            </label>
            <message>
                <!-- %mask вместо %msg — маскируем данные и в Loki -->
                <pattern>{
                    "@timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}",
                    "level": "%level",
                    "logger": "%logger{36}",
                    "thread": "%thread",
                    "traceId": "%X{traceId:-}",
                    "userId": "%X{userId:-}",
                    "requestId": "%X{requestId:-}",
                    "message": "%mask",
                    "exception": "%ex{full}"
                    }</pattern>
            </message>
            <sortByTime>true</sortByTime>
        </format>
        <batchConfig>
            <maxItems>${LOKI_BATCH_SIZE}</maxItems>
            <timeoutMs>${LOKI_BATCH_TIMEOUT}</timeoutMs>
        </batchConfig>
    </appender>

    <springProfile name="prod,production">
        <logger name="com.padle.core" level="INFO"/>
        <logger name="org.springframework.security" level="WARN"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="LOKI"/>
        </root>

        <logger name="com.padle.core.service.PaymentService" level="INFO" additivity="false">
            <appender-ref ref="JSON_FILE"/>
        </logger>
    </springProfile>

    <!-- ========== КОРНЕВОЙ ЛОГГЕР ========== -->

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

    <!-- ========== ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ ========== -->

    <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener"/>

    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <!-- Конвертеры — clr/wex/wEx оригинальные + mask новый -->
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex" class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx" class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <!-- Паттерны: %mask вместо %msg — единственное изменение по сравнению с оригиналом -->
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} %clr(%-5level) %clr(${PID:-}){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %mask%n%wEx"/>
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss} %-5level ${PID:-} --- [%t] %-40.40logger{39} : %mask%n%wEx"/>

</configuration>